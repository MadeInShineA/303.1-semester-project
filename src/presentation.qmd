---
title: "303.1 Semester Project Presentation"
format:
  revealjs:
    theme: default
    center: true
    transition: fade
    incremental: false
    slide-number: true
    preview-links: auto
    navigation-mode: grid
    auto-play-media: true
    footer: '<a href="./index.qmd" style="color: inherit; text-decoration: none;">Home</a> | <a href="./sources.qmd" style="color: inherit; text-decoration: none;">Sources</a> | 303.1 Semester Project Presentation © 2025'
---


# What is DWI ?

Diffusion Weighted Imaging (DWI) is an MRI technique that measures the diffusion of water molecules in biological tissues.

- **Principle**: Uses magnetic field gradients to sensitize the MRI signal to water diffusion
- **Key parameters**: b-values and b-vectors

# What's DWI ? 


::: {#fig-cern}

{{<video ./assets/presentation/dMRI-signal-movie.mp4 height="500">}}

Visual example of DWI, sourced from Dr. A. Rokem, DIPY Workshop 2021

:::
# DWI Pipeline: Acquisition

```{dot}
//| label: fig-dwi-pipeline-acquisition
//| fig-cap: "Overview of the DWI Pipeline (Acquisition)."
digraph DWI_Pipeline {
  rankdir=LR;
  node [style=filled, fillcolor=lightblue, fontcolor=black, fontsize=10];
  edge [fontsize=10];

  subgraph cluster_acq {
      label="Acquisition";
      style=filled;
      color=lightblue;
      MRI [label="MRI\nScanner"];
      Params [label="b-values &\nb-vectors"];
      Gradients [label="Apply\nDiffusion\nGradients"];
      Signal [label="Signal Images"]
      MRI -> Gradients;
      Params -> Gradients;
      Gradients -> Signal;
  }
}
```
# DWI Pipeline: Preprocessing

```{dot}
//| label: fig-dwi-pipeline-preprocessing
//| fig-cap: "Overview of the DWI Pipeline (Preprocessing)."
digraph DWI_Pipeline {
  rankdir=LR;
  node [style=filled, fillcolor=lightblue, fontcolor=black, fontsize=10];
  edge [fontsize=10];


  subgraph cluster_prep {
      label="Preprocessing";
      style=filled;
      color=lightgreen;
      Signal [label="Signal Images"]
      Eddy [label="Eddy Current\nCorrection"];
      Motion [label="Motion\nCorrection"];
      Denoise [label="Denoised\n Signal Images"];
      Signal -> Eddy -> Motion -> Denoise;
  }
}
```

# DWI Pipeline: Reconstruction
```{dot}
//| label: fig-dwi-pipeline-reconstruction
//| fig-cap: "Overview of the DWI Pipeline (Reconstruction)."
digraph DWI_Pipeline {
  rankdir=LR;
  node [style=filled, fillcolor=lightblue, fontcolor=black, fontsize=10];
  edge [fontsize=10];

   subgraph cluster_rec {
       label="Reconstruction";
       style=filled;
       color=lightsalmon;
       Denoise [label="Denoised\n Signal Images"];
       DTI [label="DTI"];
       CSD [label="CSD"];
       GQI [label="GQI", fillcolor=yellow, shape=box];
       Tensors [label="Diffusion\nTensors"];
       ODFs [label="ODF\nData"];
       Denoise -> DTI;
       Denoise -> CSD;
       Denoise -> GQI;
       DTI -> Tensors;
       CSD -> ODFs;
       GQI -> ODFs;
   }
}
```
# DWI Pipeline: Analysis
```{dot}
//| label: fig-dwi-pipeline-analysis
//| fig-cap: "Overview of the DWI Pipeline (Analysis)."
digraph DWI_Pipeline {
  rankdir=LR;
  node [style=filled, fillcolor=lightblue, fontcolor=black, fontsize=10];
  edge [fontsize=10];

   subgraph cluster_ana {
       label="Analysis";
       style=filled;
       color=lightcoral;
       Tensors [label="Diffusion\nTensors"];
       ODFs [label="ODF\nData"];
       Tractography [label="Tractography"];
       FA [label="FA\nMaps"];
       ODF [label="ODF\nVisualization"];
       Tensors -> FA;
       ODFs -> Tractography;
       Tractography -> ODF;
   }
}
```


# DWI Pipeline: Complete pipeline
```{dot}
//| label: fig-dwi-pipeline-complete
//| fig-cap: "Overview of the DWI complete Pipeline."
digraph DWI_Pipeline {
  rankdir=LR;
  node [style=filled, fillcolor=lightblue, fontcolor=black, fontsize=10];
  edge [fontsize=10];

  subgraph cluster_acq {
      label="Acquisition";
      style=filled;
      color=lightblue;
      MRI [label="MRI\nScanner"];
      Params [label="b-values &\nb-vectors"];
      Gradients [label="Apply\nDiffusion\nGradients"];
      Signal [label="Signal Images"]
      MRI -> Gradients;
      Params -> Gradients;
      Gradients -> Signal;
  }

  subgraph cluster_prep {
      label="Preprocessing";
      style=filled;
      color=lightgreen;
      Eddy [label="Eddy Current\nCorrection"];
      Motion [label="Motion\nCorrection"];
      Denoise [label="Denoised\n Signal Images"];
      Signal -> Eddy -> Motion -> Denoise;
  }

   subgraph cluster_rec {
       label="Reconstruction";
       style=filled;
       color=lightsalmon;
       DTI [label="DTI"];
       CSD [label="CSD"];
       GQI [label="GQI", fillcolor=yellow, shape=box];
       Tensors [label="Diffusion\nTensors"];
       ODFs [label="ODF\nData"];
       Denoise -> DTI;
       Denoise -> CSD;
       Denoise -> GQI;
       DTI -> Tensors;
       CSD -> ODFs;
       GQI -> ODFs;
   }

   subgraph cluster_ana {
       label="Analysis";
       style=filled;
       color=lightcoral;
       Tensors [label="Diffusion\nTensors"];
       ODFs [label="ODF\nData"];
       Tractography [label="Tractography"];
       FA [label="FA\nMaps"];
       ODF [label="ODF\nVisualization"];
       Tensors -> FA;
       ODFs -> Tractography;
       Tractography -> ODF;
   }
}
```

# Project Goals

  - Contributing to [DIPY](https://dipy.org/)
  - Implementing a predict function to the GQI model

# Impact

  - Head motion noise reduction
  - Meaningful data augmentation
  - Model cross validation

# What was implemented

# How things were tested

# Obtained Results

# Conclusion

# Additional Resources

If you want more information please visit this project [website](./index.qmd){target="_blank"}

![Project website QR code](./assets/presentation/qrcode.png){#fig-qr}
